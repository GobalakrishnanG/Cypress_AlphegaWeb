{
  "name": "cypress_mocha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --spec 'cypress/integration/login.feature' --env allure=true",
    "test:all": "npx cypress run --spec '**/*.features'",
    "report:allure": "allure generate allure-results --clean && allure open allure-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.4.1",
    "cypress-cucumber-preprocessor": "^4.2.0",
    "cypress-wait-until": "^1.7.1"
  },
  "dependencies": {
    "@shelex/cypress-allure-plugin": "^2.26.3",
    "allure-commandline": "^2.17.2",
    "npx": "^10.2.2",
    "popper.js": "^1.14.3"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/support/step_definitions/"
  }
}
